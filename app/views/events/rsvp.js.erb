// Add this rsvp to the list for the event. The id is always named rsvp_list<eventid>
$("<%= escape_javascript(render @rsvp) %>").appendTo("#rsvp_list<%=@rsvp.event.id%>");
$("#yidTypeAhead<%= @rsvp.event.id %>").val("");
